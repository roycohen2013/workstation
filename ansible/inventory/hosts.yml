---
# `make apply` targets this machine directly. Packer supplies its own
# inventory for the build VM, so this file only describes the local case.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
